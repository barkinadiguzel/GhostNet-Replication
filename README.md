# GhostNet-Replication
PyTorch implementation of GhostNet for efficient image classification and lightweight deep learning models. Includes Ghost Module and Ghost Bottleneck for reduced computation while maintaining accuracy.
